1.创建一个主场景的总脚本
可以使用Node.getComponent('script')获取组件
var node = this.aNode.getCompont('logo')
后面就可以直接用这个，调用这个脚本里的函数
如node.add()

2.setScale,setPostion

3.按钮上绑定脚本函数如：
test(target,data){
  target表示了当前的节点(这个按钮),
  data则是一个自定义的一个东西，界面上有
  就可以用一个函数来判断是哪个按钮
}

4.node.stopAllActions停止所有动作

总结:做几个按钮控制logo的移入移除


关卡动画:
使用五个节点，用数组设置
中间的节点设置一个当前是第几关的属性
每个节点都有一个是否是boss关的属性

根据中间节点设置所有节点的label数字
小于等于0就active=false
opacity =0-255控制透明度
出现和消失都使用淡入淡出fadeIn  fadeOut
